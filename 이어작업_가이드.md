# ⚡ 번개 추적기 — 이어작업 가이드

> 새 대화창(Claude Code)에서 이 프로젝트를 이어서 작업할 때, **이 파일을 먼저 읽어달라고 하면** 맥락을 바로 파악할 수 있어요.
> 사용법: 새 대화창에서 → "번개 추적기 이어서 작업할래. `이어작업_가이드.md` 읽어줘"

---

## 1. 이 프로젝트가 뭔가요

**동탄이네 천둥번개 알림이**: 천둥번개가 다가오는 것을 **웹푸시로 알려주는** 무료 알림 서비스.

⚠️ **서비스 정체성 (문구 작성 시 반드시 지킬 것)**
- **알림 서비스일 뿐.** 치료·극복·훈련 서비스가 **아님**
- 목적은 딱 하나: 보호자가 **무방비로 천둥소리를 맞지 않도록 준비할 시간 확보**
- **낙뢰는 예보 불가** → 예측이 아니라 **이미 친 낙뢰의 접근을 감지**해서 전달
- **"실시간" 아님** → 최대 5분 지연(우리 cron 주기). 단, 기상청 지점 데이터 자체는 **초 단위**(10분 단위 아님 — 실측 확인). 10분 단위는 날씨누리 화면·격자 상품 쪽
- 금지 표현: "둔감화를 위한", "적응시키는", "공포를 줄여주는", "실시간"
- 만든 계기: 반려견 **동탄이**의 천둥 공포

## 2. 코드가 있는 곳 (2곳, 안전하게 저장됨)

| 위치 | 경로 |
|------|------|
| **내 PC 폴더** | `C:\Users\USER\OneDrive\문서\thunder-alert-saas` |
| **GitHub (클라우드)** | `kustomduo-oss/thunder-miri` (Public) |
| **라이브 사이트** | https://kustomduo-oss.github.io/thunder-miri/ |

## 3. 새 대화창에서 이어가는 법

1. 새 Claude Code 대화창을 **위 PC 폴더**에서 열기
2. "번개 추적기 이어서 작업할래" 라고 말하기
3. Claude가 **메모리**(`thunder-alert-saas-idea.md`)와 **이 파일**을 읽고 맥락 파악
4. 수정 후 → **`git push`** 하면 실제 서비스에 자동 반영

```bash
cd "C:\Users\USER\OneDrive\문서\thunder-alert-saas"
git add . && git commit -m "수정 내용" && git push
```
> `git push` = 수정한 코드를 GitHub에 "제출". 넣는 순간 자동으로 새 코드로 알림이 나가요.

## 4. 핵심 파일 구조

| 파일 | 역할 |
|------|------|
| `index.html` | 가입 화면 (강아지 이름·위치·알림간격 입력, 웹푸시 구독) |
| `sw.js` | 서비스워커 (푸시 알림을 폰에 띄움) — **함부로 수정 주의** |
| `manifest.json` | PWA 설정 (아이폰 홈화면 추가용) |
| `sender/sender.py` | **발송 엔진** (기상청 조회 → 알림 발송). 핵심 로직 대부분 여기 |
| `.github/workflows/send.yml` | GitHub Actions (발송 엔진을 클라우드에서 실행) |
| `nav.js` | 모든 페이지 공통 헤더/푸터 (메뉴 수정은 여기 한 곳) |
| `supabase_schema.sql` | DB 구조 (기록용) |
| `sender/.env.secret` | 로컬 비밀키 (git 제외, PC에만) |
| 정책/콘텐츠 | `privacy/terms/disclaimer/cookies/about/story/how/guide*/blog.html` |

## 5. 서비스가 돌아가는 구조

```
cron-job.org (5분마다)
   └─→ GitHub Actions(send.yml) 실행
        └─→ sender.py: Supabase에서 구독자 읽기
             └─→ 기상청 API로 낙뢰·강수 확인
                  └─→ 조건 맞으면 웹푸시 발송
```
- **DB**: Supabase (프로젝트 ref `pdlohzenslwbiyoxwjom`)
- **스케줄러**: cron-job.org (GitHub 내장 cron이 몇 시간씩 밀려서, 외부 스케줄러로 5분마다 확실히 실행. GitHub 내장 schedule은 중복 발송 방지를 위해 제거됨)

## 6. 알림 로직 (중요)

**⚡ 낙뢰 경보** (예측 불가 = 관측 기반, 가입자 간격과 **독립**)
- 30km 이내 → `watch`(접근), 10km 이내 → `warning`(임박)
- 단계 상승(접근→임박, 또는 처음) → **즉시 발송**
- 같은 단계 유지 → 30분 간격 (`LIGHTNING_COOLDOWN_MIN`)
- 문구에 거리 표기: `🐾⚡ 천둥 코앞 (약 8km)`

**🌧 강수 예보** (예측 가능 = 예보 기반, 가입자가 간격 선택)
- 앞으로 **3시간** 내 강수 예보 확인 (`RAIN_FORECAST_HOURS=3`)
- 가입자가 고른 간격(10/20/30분, `cooldown_min`)마다
- 문구: `🐾🌧 곧 비 올 듯` + `약 N분 뒤(HH:MM쯤) 비`

## 7. 꼭 기억할 함정 ⚠️

1. **시간대**: GitHub Actions는 UTC라 `send.yml`에 `TZ: Asia/Seoul` 필수 (없으면 예보 시각 9시간 어긋남). **로컬(KST)은 정상이라 안 드러남 → 검증은 반드시 클라우드 로그로.**
2. **로컬 테스트**: `sender/.env.secret`에 비밀키(KMA/Supabase/VAPID) 있어야 함. `PYTHONIOENCODING=utf-8` 붙여 실행(한글 출력).
3. **Supabase 새 키**: `apikey` 헤더에만. `Prefer: return=representation` 쓰지 말 것(RLS 위반).
4. **cron-job.org**: 폼에서 method/헤더 바꾸면 body가 리셋됨 → body 비면 GitHub 422. 저장 후 body 재확인.
5. **죽은 구독**: 아이폰 앱 재설치/알림 재설정 시 구독이 조용히 무효화됨(발송은 "성공"으로 뜨지만 폰엔 안 옴). 발송 중 404/410이면 자동 비활성화됨.

## 8. 로컬에서 직접 테스트하는 법 (참고)

```bash
cd "C:\Users\USER\OneDrive\문서\thunder-alert-saas"
# 현재 낙뢰/예보 확인만 (발송 안 함)
PYTHONIOENCODING=utf-8 python -c "import os,sys; sys.path.insert(0,'sender'); import sender; sender.load_local_env(); sender.KMA_API_KEY=os.environ.get('KMA_API_KEY','').strip(); print(sender.fetch_forecast(69,106))"
```
GitHub Actions 수동 실행: `gh workflow run send.yml --repo kustomduo-oss/thunder-miri --ref main -f mode=once`

## 9. 현재 상태 / 남은 것

- ✅ 전체 가동 중 (가입→DB→cron-job.org→GitHub Actions→웹푸시)
- ✅ 낙뢰/강수 분리, 가입자별 간격, 거리 표기, 시간대 수정 완료
- 🔲 **죽은 구독 정리** + 사용자 폰 재등록 (구독이 4→3→2로 줄어듦)
- 🔲 커스텀 도메인 (선택)
- 🔲 콘텐츠 더 쌓기 (애드센스 승인용)
- 🔲 첫 사용자 모으기

---
*이 가이드는 사람과 Claude 둘 다 읽으라고 만든 핸드오프 문서입니다. 프로젝트가 바뀌면 이 파일도 갱신하세요.*
