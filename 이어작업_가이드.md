# ⚡ 동탄이네 천둥번개 알림이 — 이어작업 가이드

> 새 대화창에서 이 프로젝트를 이어갈 때 **이 파일을 먼저 읽어달라고** 하세요.
> 사용법: "천둥번개 알림이 이어서 작업할래. `이어작업_가이드.md` 읽어줘"
>
> **최종 갱신: 2026-08-12** (작업 트리 깨끗, 전부 배포 완료, 최신 커밋 `abca19b`)

---

## 0. 지금 당장 할 일 (다음 세션 시작점) 🔴

사용자가 **"다른 곳도 보고 한 번에 수정하자"** 며 대기시킨 상태입니다. **아직 수정하지 마세요.**

### 확정된 수정 사항 (합의 완료, 실행 대기)
1. **네비게이션에서 "시작 전 확인" 탭 제거** — `nav.js`의
   `'<a class="cta" href="index.html#sound-check">시작 전 확인</a>'` 줄 삭제.
   (소리 확인이 더 이상 필수가 아니라 메뉴 CTA로 부적절. 사용자가 "없애는 걸로" 확정)
2. **첫 화면 레이아웃 정리** — 실측으로 확인된 문제:

| 요소 | 현재 위치(브라우저 높이 911px 기준) | 문제 |
|------|------|------|
| 헤드라인 | 높이 **340px** | 화면의 37% 차지 |
| 가입 폼 시작 | 651px | 화면 하단에서야 시작 |
| **가입 버튼(ctaBtn)** | **1092px** | ❌ **첫 화면 밖** — 핵심 CTA가 안 보임 |
| 좌우 높이 | 왼쪽 1069 / 지도 656 | **413px 불균형**, 오른쪽 아래 빔 |

   제안했던 수치(승인 전):
   ```
   헤드라인   clamp(48px,6.5vw,76px) → clamp(34px,4vw,52px)   (340 → 약 200px)
   히어로 여백 96px → 56px
   폼 패딩    32px → 24px
        → 가입 버튼 1092 → 약 800px (첫 화면 안)
   ```

### 아직 점검 안 한 곳 (사용자에게 어디부터 볼지 물어볼 것)
- **모바일 화면** ← 오늘 한 번도 확인 안 함. 실사용자 대부분이 폰. **가장 우선 추천**
- `radar.html`이 첫 화면과 중복인지 판단 (사용자: "결과물 보고 없앨지 그때 결정")
- 하위 페이지들(도움말·이야기·참고자료 등) — 문구만 고치고 화면은 미확인
- 홈 아래쪽 섹션 순서·흐름

---

## 1. 이 서비스가 뭔가 (정체성 — 문구 쓸 때 최우선 준수) ⭐

**천둥번개가 다가오는 것을 웹푸시로 알려주는 무료 알림 서비스.**

- ✅ 목적은 **딱 하나**: 보호자가 **무방비로 천둥소리를 맞지 않도록 준비할 시간 확보**
- ❌ 치료·극복·훈련 서비스가 **아님**
- **낙뢰는 예보 불가** → 예측이 아니라 **이미 친 낙뢰의 접근을 감지**해 전달
- **"실시간" 아님** → 최대 5분 지연(우리 조회 주기). 정확한 표현: *"이미 친 낙뢰를 최대 5분 안에 확인해 알려드립니다"*
- **금지 표현**: "둔감화를 위한", "적응시키는", "공포를 줄여주는", "실시간", 효과 단정("충격이 훨씬 적습니다" 류)
- **소리 적응은 '선택 안내'일 뿐 — 절대 가입을 막지 말 것.** 불안 반응이 있어도 "소리 없이 알림만 이용 가능"으로 안내

> ⚠️ **2026-08-11 사용자 자기수정 (중요한 맥락):** 한때 "유튜브 천둥소리 적응 훈련"이 주력이라 착각해, 가입 전에 '영상 반응 확인'을 통과해야 폼이 열리는 관문(`is-locked`)을 뒀었음. 주력은 **알림 자체**라 관문을 제거함. 같은 실수 반복 금지.

> ⚠️ **"개는 100km 밖 천둥도 느낀다"** — 근거 못 찾음(민간 통설). **사이트에 쓰지 말 것.** 흔한 "개가 초저주파를 잘 들어서" 설명도 개 청각(약 67Hz~45kHz, 저주파는 사람보다 못 들음)과 안 맞음. 검증된 건 "개의 15~30%가 천둥 전 불안 징후를 보인다" 정도.

---

## 2. 코드·배포 위치

| 위치 | 경로 |
|------|------|
| PC 폴더 | `C:\Users\USER\OneDrive\문서\thunder-alert-saas` |
| GitHub | `kustomduo-oss/thunder-miri` (Public) |
| 라이브 | https://kustomduo-oss.github.io/thunder-miri/ |
| 레이더 페이지 | https://kustomduo-oss.github.io/thunder-miri/radar.html |

```bash
cd "C:\Users\USER\OneDrive\문서\thunder-alert-saas"
git add . && git commit -m "수정 내용" && git push
```
> GitHub Pages 배포는 **50초~2분** 걸림. `curl`로 폴링해서 반영 확인할 것.

---

## 3. 파일 구조

| 파일 | 역할 |
|------|------|
| `index.html` | **첫 화면** — 히어로(카피+가입폼 / 실제 레이더 지도) + 아래 섹션들 |
| `landing.js` | 가입 로직 + **히어로 지도/레이더/타임바** 전부 |
| `landing.css` | 첫 화면 전용 스타일 |
| `radar.html` | 우리 동네 레이더 **전체 화면**(알림 탭 대상) — 자체 JS 내장 |
| `nav.js` | 모든 페이지 공통 헤더/푸터. **메뉴 수정은 여기 한 곳** |
| `style.css` | 하위 페이지 공통 스타일 |
| `sender/sender.py` | **발송 엔진** (기상청 조회 → 웹푸시) |
| `sender/radar.py` | **레이더 이미지 생성기** (관측 + 예측) |
| `sender/.env.secret` | 로컬 비밀키 (git 제외, PC에만) |
| `.github/workflows/send.yml` | GitHub Actions |
| `supabase_schema.sql` | DB 구조(기록용) |
| 하위 페이지 | `about/story/how/research/guide*/blog/privacy/terms/disclaimer/cookies/contact.html` |

---

## 4. 서비스 작동 구조

```
cron-job.org (5분마다)
   └→ GitHub Actions(send.yml) workflow_dispatch 호출
        ├→ sender.py : 구독자 읽기 → 기상청 낙뢰·강수 조회 → 웹푸시 발송
        └→ radar.py  : 레이더 격자 수신 → PNG 생성 → Supabase Storage 업로드
                        (전국판 + 구독자 동네별)
                              ↓
            index.html / radar.html 이 그 PNG를 지도에 겹쳐 표시
```

- **DB**: Supabase (ref `pdlohzenslwbiyoxwjom`), 테이블 `subscribers`
- **이미지 저장**: Supabase Storage 공개 버킷 **`radar`** (git에 넣지 말 것 — 저장소가 부풂)
- **스케줄러**: cron-job.org (GitHub 내장 schedule은 중복 발송 방지로 **제거함**)

---

## 5. 알림 로직

**⚡ 낙뢰 경보** (관측 기반, 가입자 간격과 **독립**)
- **50km 이내 → `watch`(접근)** / **30km 이내 → `warning`(임박)**
- 단계 상승(접근→임박, 또는 처음 감지) → **즉시 발송**
- 같은 단계 유지 → 30분 간격(`LIGHTNING_COOLDOWN_MIN`)
- 낙뢰 사라지면 단계 리셋 → 다음 뇌우 때 다시 즉시
- 문구에 거리 표기: `🐾⚡ 천둥 임박 (약 8km)`

**🌧 강수 예보** (예보 기반, 가입자가 간격 선택)
- 앞으로 **3시간** 내 강수 예보(`RAIN_FORECAST_HOURS=3`)
- 가입자가 고른 간격(10/20/30분, DB `cooldown_min`)
- 문구: `🐾🌧 비 예보 (약 30분 뒤)`

> 거리 수치는 `sender.py` 외에 **index.html(map-flow·process-list), how.html, radar.html(원 반경·설명)** 에도 하드코딩돼 있음. 바꿀 때 함께 고칠 것.

---

## 6. 첫 화면 구성 (2026-08-12 현재)

```
┌─ 왼쪽 ──────────────┬─ 오른쪽 ─────────────────┐
│ 카피 + 헤드라인       │ ● 상태 문구               │
│ [강아지 이름]         │ [지도 + 비구름 오버레이]    │
│ [위치: 현재/검색]     │  + 확대·드래그·스타일전환   │
│ [선택 설정(접힘)]     │ [▶] ━━●━━ 시각            │
│ [알림 받기 버튼]      │ 관측/예측 설명             │
│                     │ 50km → 30km → 영상        │
└─────────────────────┴──────────────────────────┘
```

**판단 근거(사용자):** "클릭 한 번의 허들도 없애는 게 낫다" — 두 번째 페이지까지 가는 사람은 극소수라, 서비스의 가장 강한 증거(레이더)를 첫 화면에 그대로 노출. 처음엔 Claude가 "공간이 좁다"고 반대했으나 **사용자 논리가 옳았음.**

- **전국판 필수**: 레이더는 가입자 있는 격자만 생성되므로, 가입 전 방문자용으로 `radar.py`가 **`national` 키 전국판(관측+예측)을 항상 생성**. 첫 로드는 national → 주소 입력하면 `{nx}_{ny}`로 전환(없으면 조용히 전국판 유지)
- 히어로 지도는 `radar.html`과 **동일한 조작**(확대/축소·드래그·출처·스타일 전환). 기본 **Base(일반)**, 선택은 `localStorage.thunder_basemap`을 **두 화면이 공유**
- 배경 채도 `filter:saturate(.75)`로 낮춰 강수가 도드라지게

---

## 7. 꼭 기억할 함정 ⚠️

1. **시간대(TZ)**: GitHub Actions는 UTC라 `send.yml`에 `TZ: Asia/Seoul` **필수**. 없으면 예보 시각이 9시간 어긋남. **로컬(KST)에선 정상이라 안 드러남 → 검증은 반드시 클라우드 로그로.**
2. **로컬 실행**: `PYTHONIOENCODING=utf-8 python sender/radar.py` (한글 출력). `.env.secret` 필요.
3. **Supabase 새 키**: `apikey` 헤더에만. `Prefer: return=representation` 쓰지 말 것(RLS 위반).
4. **cron-job.org**: 폼에서 method/헤더 바꾸면 **body가 리셋됨** → body 비면 GitHub 422. 저장 후 body(`{"ref":"main"}`) 재확인.
5. **죽은 구독**: 아이폰 앱 재설치/알림 재설정 시 구독이 조용히 무효화됨(발송은 "성공"으로 뜨는데 폰엔 안 옴). 404/410이면 자동 비활성화됨.
6. **범위·해상도 변경 시**: 기존 과거 프레임(`{key}_{stamp}.png`)은 좌표가 안 맞으므로 **삭제하고 meta의 `past`를 비울 것**. 안 그러면 옛 이미지가 새 bounds로 늘어나 잘못 표시됨.
7. **캐시**: Supabase Storage JSON도 캐시됨 → 검증할 땐 `?cb=타임스탬프` 붙일 것. CSS/JS 바꾸면 `index.html`의 `?v=` 값도 올려야 기존 방문자에게 반영됨.
8. **CSS 클래스 충돌**: `landing.css`의 `.cta`가 너무 광범위해 헤더 `<a class="cta">`까지 늘어난 적 있음 → `button.cta`로 한정해 해결. 새 규칙 추가 시 주의.

---

## 8. 기상청 API 정보 (어렵게 알아낸 것 — 재조사 금지)

### 승인 현황
- **레이더 카테고리 11건** 승인 (~2028-08-11) — API허브 `seqApi=5`
- **초단기예측(QPF)** 승인 — API허브 **`seqApi=9&seqApiSub=282`** (수치모델 > 초단기예측). ⚠️ 레이더와 **별개 카테고리**라 따로 신청했음
- 낙뢰관측은 원래부터 사용 중

### 레이더 합성영상 (관측)
```
https://apihub.kma.go.kr/api/typ01/cgi-bin/url/nph-rdr_cmp1_api
params: tm(YYYYMMDDHHMI, 생략시 종종 "자료 없음" → 명시 권장), cmp=HSR, qcd=KMA,
        obs=ECHO, map=HB, disp=B(binary 13MB / A=ASCII 47MB)
응답: 앞 4byte = nx,ny(little-endian short), 이후 nx*ny개 short
     row 0 = 남쪽(flipud 필요), 값 = dBZ*100
     -30000 = 관측영역 밖, -25000 = 에코 없음
map=HB 격자 = 2305 x 2881, 0.5km (1152.5 x 1440.5 km)
```

### ⭐ 좌표 투영 (제일 어려웠던 부분, 검증 완료)
```
+proj=lcc +lat_1=30 +lat_2=60 +lat_0=38 +lon_0=126 +x_0=0 +y_0=0 +ellps=WGS84 +units=km
격자중심 = 투영원점.  ix = x/0.5 + nx/2,  iy = y/0.5 + ny/2
```
검증: 청주·서울·부산·제주·울릉도 전부 관측영역 안, 동해먼바다(133.5E)·중국(113E)은 격자 밖. `pip install pyproj` 필요.

### 미래 강수예측 QPF (예측)
```
https://apihub.kma.go.kr/api/typ03/cgi/dfs/nph-qpf_ana_img
params: eva=2, tm(10분단위), qpf=B(융합예측; K=초단기모델, M=레이더외삽),
        ef=예측시간(분, 30~360), map=HB, grid=2, legend=0, size=600
```
- **응답은 기상청이 렌더한 완성 PNG**(해안선·범례 포함, 불투명)
- ⭐ **좌표 실측 확정**: `map=HB, legend=0, size=600` → **600 x 770 PNG**.
  **20행·769행이 지도 테두리선**(가로 600px 전체가 검은 선) → 지도영역 = **rows 20..769(750px) x cols 0..599(600px)**.
  이 750x600 영역이 **레이더 HB 격자와 동일**(종횡비 0.8001 vs 0.8).
  ```
  col = (x_km + 1152.5/2)/1152.5*600
  row = 20 + (1440.5/2 - y_km)/1440.5*750
  ```
  검증: 서울·부산·울릉도는 해안선 위, 서해/동해 먼바다는 빈 곳. 생성된 예측 bounds가 관측 bounds와 **0.3km 오차**.
- **투명 오버레이 변환**: 그림에 해안선·배경이 있으므로 **채도(max−min RGB) > 30인 픽셀만 = 비**로 골라내고 나머지 투명 처리. 그래야 우리 지도에 겹침
- ef=0은 거의 빈 이미지(782B)라 제외. 유효 프레임은 3000바이트 초과로 판정

### ⚠️ 절대 쓰지 말 것
기상청 날씨누리의 GIS 모드는 **내부 전용 API**(`vapi.kma.go.kr/capi/url/vs_10minutes_rain_timeline.php`)를 씀. API허브에 없는 비공개 주소라 **무단 사용 금지**(언제든 차단·변경 가능, 광고 수익 서비스엔 특히 위험). 우리는 공개 API PNG를 직접 좌표보정해 해결했음.

### 낙뢰 자동 이동방향 판별 = 불가 (검증 완료, 재시도 말 것)
- 5분 창 최단거리가 ±40~60km씩 튐(뇌우 안에서 번개가 무작위로 터지는 점 과정)
- 20~30분 창으로 평균내면 추세는 보이나 판단이 20~30분 늦어 경보용 부적합
- → **강수 레이더(덩어리로 움직임)가 방향 판단의 핵심.** 과거(낙뢰)와 미래(레이더)는 충돌이 아니라 상호보완

---

## 9. 브이월드(국토교통부) 지도

- 키: `12A21CCD-E7FC-3E4E-B6A5-F8DE7A32A24D` (도메인 `kustomduo-oss.github.io` 잠김 → 공개돼도 안전. 브라우저 코드에 넣는 용도)
- ⚠️ **개발키라 2027-02-11 만료**, 최대 3회 연장. **만료되면 지도가 조용히 안 뜸.** 운영키(2년)는 심사 필요
- 타일: `https://api.vworld.kr/req/wmts/1.0.0/{KEY}/{TYPE}/{z}/{y}/{x}.png`
- 사용 가능 TYPE 실측(**브라우저에서 테스트할 것** — curl은 도메인 제한으로 무의미):
  - ✅ `Base`(일반) · `white`(밝게) · `midnight`(어둡게) · `Hybrid`
  - ❌ `Satellite`(권한 없음, 추가 신청 필요) · `gray`(서비스 종료)
- 실패 시 OpenStreetMap으로 자동 폴백 구현돼 있음

---

## 10. Supabase Storage 파일 규칙

| 파일 | 내용 |
|------|------|
| `national.png` / `national.json` | 전국 관측(가입 전 방문자용) |
| `national_forecast.json` + `national_qpf_{ef}.png` | 전국 예측 9프레임 |
| `{nx}_{ny}.png` / `.json` | 동네 관측(최신) |
| `{nx}_{ny}_{YYYYMMDDHHMM}.png` | 과거 프레임(최대 7개, `_keep_recent_frames`가 초과분 삭제) |
| `{nx}_{ny}_forecast.json` + `{nx}_{ny}_qpf_{ef}.png` | 동네 예측 |

- meta JSON에 `bounds`(위경도 4모서리)·`home`·`rain_cells`·`max_dbz`·`observed_at`·`lightning[]`·`past[]` 포함
- 예측 프레임엔 `rain_px` — 0이면 화면에 "이 시각엔 예보된 비가 없습니다" 표시(고장 오인 방지)
- 공개 URL: `{SUPABASE_URL}/storage/v1/object/public/radar/{파일명}`

### ⭐ 위경도 재투영 필수
LCC 격자를 위경도 사각형에 그대로 얹으면 좁을 땐 괜찮지만 **넓히면 한반도가 비뚤어짐**.
`view_bounds()`(LCC 상자 네 변을 훑어 실제 min/max 위경도) + `latlon_sample_grid()`(출력 위경도 격자 → LCC 역변환 후 샘플링)로 **관측·예측 둘 다 동일한 위경도 격자에 재투영**. 표시 범위 = 우리집 **±520km**(`RADAR_CROP_KM`), 출력 **900px**.

---

## 11. 사용자 작업 스타일 (중요)

- **한국어로 응답**
- 웹개발이 처음 → **매 단계 "지금 뭐 하는 중인지 + 쉬운 비유"** 곁들일 것
- 승인 문화: 코드 수정 전 **제안 → 승인** 받고 진행 ("ㅇㅇ", "해봐", "진행해" 등)
- **추측하지 말고 실제로 측정·검증**하는 걸 선호 (오늘 여러 번 "실측했더니 가설이 틀렸다"로 방향이 바뀜)
- 잘못된 제안을 하면 **정직하게 인정하고 방향 수정**할 것 (오늘 여러 번 사용자 판단이 더 옳았음)
- 사업 관점: **체류시간 → 광고 수익**을 중시. "알림만 받고 끝나면 서비스가 거기서 끝난다"

---

## 12. 확장 시 병목 (미리 알아둘 것)

- 첫 병목은 서버가 아니라 **`sender.py`의 순차 발송 루프(약 1,000~2,000명)** → 병렬 발송으로 개선하면 1만명대까지 감당
- GitHub Pages/Supabase/Actions는 훨씬 나중 문제
- 1만명+ 되면 Cloudflare(Workers+R2+Pages) 검토 — 그때쯤 광고 수익이 비용을 넘음

---

## 13. 남은 할 일 (0번 이후)

- 🔲 **모바일 화면 점검** (미확인, 최우선)
- 🔲 죽은 구독 정리 (구독자가 4→3→2로 줄어든 이력)
- 🔲 `radar.html` 중복 여부 판단
- 🔲 콘텐츠 더 쌓기 (애드센스 승인용)
- 🔲 커스텀 도메인
- 🔲 브이월드 위성영상 API 추가 신청(원하면)
- 🔲 첫 사용자 모으기

---
*이 문서는 사람과 Claude 둘 다 읽으라고 만든 핸드오프 문서입니다. 프로젝트가 바뀌면 이 파일도 갱신하세요.*
