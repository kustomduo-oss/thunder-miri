# ⚡ 썬더미리 — 이어작업 가이드

> 새 대화창에서 이 프로젝트를 이어갈 때 **이 파일을 먼저 읽어달라고** 하세요.
> 사용법: "썬더미리 이어서 작업할래. `이어작업_가이드.md` 읽어줘"
>
> **최종 갱신: 2026-08-21** · 기상청 itv=60 함정 수정(화면 낙뢰 복구)·레이더 실행 취소 해결·분석도구·og 배너·인스타 DM 창구 반영
> ⚠️ 이 문서가 코드보다 오래됐을 수 있음. 시작 전 `git log --oneline -15`로 최근 커밋을 먼저 확인할 것.

---

## 1. 이 서비스가 뭔가 (정체성 — 문구 쓸 때 최우선 준수) ⭐

**썬더미리** (헤더 표기: "동탄이네 썬더미리")
= 우리 동네로 다가오는 **천둥번개(낙뢰)를 웹푸시로 미리 알려주는** 무료 알림 서비스.

- ✅ 목적은 **딱 하나**: 보호자가 **무방비로 천둥소리를 맞지 않도록 준비할 시간 확보**
- ❌ 치료·극복·훈련 서비스가 **아님**
- **낙뢰는 예보 불가** → 예측이 아니라 **이미 친 낙뢰의 접근을 감지**해 전달
- **"실시간" 아님** → 최대 5분 지연(조회 주기). 정확한 표현: *"기상청 낙뢰 관측 정보를 5분마다 확인"*
- **금지 표현**: "둔감화를 위한", "적응시키는", "공포를 줄여주는", "실시간", 효과 단정("충격이 훨씬 적습니다" 류)
- **소리 적응은 '선택 안내'일 뿐 — 절대 가입을 막지 말 것**

> **첫 화면 문구 (2026-08-19 교체) — 이 톤을 기준으로 삼을 것**
> ```
> 천둥번개가 오기 전에,
> 보호자에게
> 준비할 시간을 드려요.
>
> 우리 동네 50km 안에서 번개가 관측되면 알려드려요.
> 천둥번개가 다가오는 중인지 알 수 있습니다.
> ```
> 옛 문구("우리 동네로 다가오는 천둥번개, 보호자에게 미리 알려드려요")는 **번개 자체를 예측해준다는 오해**를 줘서 교체함.
> - "미리"가 거짓은 아니다 — 기준점이 *번개 발생*이 아니라 **우리 동네 도착**이기 때문. 다만 그 구조가 문구에서 안 보였다
> - 근거: `sender.py:69` 주석 — 뇌우 이동속도 20~60km/h 기준 **50km면 약 50분~2.5시간의 여유**
> - 팔아야 할 것은 "번개를 안다"가 아니라 **"아이를 안고 있을 시간을 번다"**

> ⚠️ **과거 실수 (반복 금지):** 한때 "유튜브 천둥소리 적응 훈련"이 주력이라 착각해, 가입 전에 '영상 반응 확인'을 통과해야 폼이 열리는 관문(`is-locked`)을 뒀었음. 주력은 **알림 자체**라 제거함.

> ⚠️ **"개는 100km 밖 천둥도 느낀다"** — 근거 못 찾음(민간 통설). **사이트에 쓰지 말 것.** "개가 초저주파를 잘 들어서"라는 흔한 설명도 개 청각(약 67Hz~45kHz, 저주파는 사람보다 못 들음)과 안 맞음. 검증된 건 "개의 15~30%가 천둥 전 불안 징후를 보인다" 정도.

---

## 2. 코드·배포 위치

| 위치 | 경로 |
|------|------|
| PC 폴더 | `C:\Users\USER\OneDrive\문서\thunder-alert-saas` |
| GitHub | `kustomduo-oss/thunder-miri` (Public) |
| **라이브** | **https://thundermiri.com** (가비아 구입, 2026-08-16 연결) |
| 옛 주소 | `kustomduo-oss.github.io/thunder-miri/` → 새 도메인으로 301 자동 이동 |

> 저장소·Supabase 프로젝트 등 **내부 식별자는 계속 `thunder-miri`**. 표시명만 썬더미리.

**도메인 관련 주의**
- DNS는 가비아: A 레코드 4개(`185.199.108~111.153`) + `www` CNAME `kustomduo-oss.github.io.` (끝 점 필수)
- 저장소 루트의 `CNAME` 파일에 `thundermiri.com` — 지우면 도메인이 풀린다
- ⚠️ **주소가 하위 경로에서 루트로 바뀌었다.** 웹푸시 구독·localStorage·서비스워커 scope는 도메인 단위라 옛 주소의 구독은 **전부 무효**. 아이폰 홈화면 앱도 다시 추가해야 함
- ⚠️ **브이월드 지도 키는 도메인 잠금**이라 새 도메인을 인증키 설정에 추가해야 지도가 뜬다(2026-08-16 추가 완료)

```bash
cd "C:\Users\USER\OneDrive\문서\thunder-alert-saas"
git add . && git commit -m "수정 내용" && git push
```
> GitHub Pages 배포는 **50초~2분**. `curl`로 폴링해 반영 확인할 것.

---

## 3. 파일 구조

| 파일 | 역할 |
|------|------|
| `index.html` | **메인 (거의 모든 것)** — 히어로(카피 / 레이더 3탭 / 가입폼) + 알림설정 모달 + 더알아보기 |
| `landing.js` | 가입 + 히어로 지도·낙뢰재생·강수예측·산책날씨 **전부** (약 900줄) |
| `landing.css` | 메인 화면 스타일 |
| `nav.js` | 공통 헤더/푸터. **메뉴 수정은 여기 한 곳** |
| `style.css` | 하위 페이지 공통 |
| `sender/sender.py` | **발송 엔진** — 낙뢰만 판단·발송 |
| `sender/radar.py` | **레이더 이미지 생성기** (관측 + 예측 + 낙뢰 JSON) |
| `og-image.png` | 링크 공유용 배너 1200×630. 동탄이 스티커 합성본 |
| `sitemap.xml` · `robots.txt` | 검색엔진용. 페이지를 늘리면 sitemap도 함께 고칠 것 |
| `404.html` | 없는 주소 안내. `noindex` |
| `sender/cleanup_past_frames.py` | Storage의 옛 과거 프레임 정리 도구(세기/삭제). 평소엔 안 씀 |
| `sender/.env.secret` | 로컬 비밀키 (git 제외) |
| `.github/workflows/send.yml` | 푸시 발송 |
| `.github/workflows/radar.yml` | 레이더 생성 (send 성공 후 자동) |
| 하위 페이지 | `story/guide-training/about/contact/privacy/terms/disclaimer/cookies/how/guide/research/blog.html` |
| 아이콘 | `thundermiri-icon-192/512.png`, `thundermiri-apple-touch-icon.png` |

---

## 3-1. 공유·검색 노출 (2026-08-19 신설)

### og 태그 — 15개 페이지 전부
카톡·카페에 링크를 붙였을 때 뜨는 미리보기 카드를 만든다. 없으면 파란 주소 한 줄만 나온다.

- 제목·설명은 **각 페이지의 `<title>`과 `meta description`을 그대로** 가져다 쓴다.
  따로 관리하면 나중에 한쪽만 고쳐져 어긋난다
- 이미지: `og-image.png` (1200×630). **동탄이 스티커 합성본**이며 원본은 ChatGPT로 만든 1254×1254 스티커
- 재합성이 필요하면 스크래치 스크립트 대신 아래 규격만 지키면 된다
  | 항목 | 값 |
  |---|---|
  | 크기 | 1200×630 (1.91:1) |
  | 안전영역 | 카톡이 좌우를 자를 수 있어 핵심 요소는 **가운데 60%** 안 |
  | 글자 | AI 이미지 모델은 한글을 못 쓴다. **그림만 만들고 글자는 따로 얹을 것** |
  | 색 | 배경 `#f4f8fb`, 잉크 `#102334`, 포인트 `#0879b9` |
- ⚠️ **페이지를 새로 만들면 og 태그도 같이 넣어야 한다.** `<title>`만 넣고 끝내면 그 페이지만 카드가 안 뜬다

### 문의 창구
| 창구 | 주소 | 성격 |
|---|---|---|
| **인스타그램 DM** | `https://ig.me/m/dongtan2ne` | **첫 창구.** `ig.me/m/`은 프로필이 아니라 DM 창이 바로 열린다 |
| 이메일 | kustomduo@gmail.com | 보조 |

`contact.html`과 `nav.js` 푸터 두 곳에 있다. 아이디가 바뀌면 둘 다 고칠 것.

### sitemap · robots · 404
- `sitemap.xml` — 15개 URL. `lastmod`는 **각 파일의 마지막 커밋 날짜**로 넣었다
- ⚠️ **페이지를 늘리거나 지우면 sitemap도 함께 고친다.** 자동 생성이 아니다
- `404.html` — GitHub Pages가 자동으로 쓴다. `noindex` 달려 있음
- 🔲 아직 안 함: **Google Search Console에 sitemap 제출**

### 메뉴 구조 (2026-08-19 개편)
```
헤더  우리 동네 레이더 / 알림 받기 / 작동 방식 / 동탄이네 이야기
푸터  소개 / 문의 / 개인정보처리방침 / 쿠키 고지 / 이용약관 / 면책 고지
```
- 순서 의도: **뭐지?(작동 방식) → 받아볼까?(알림) → 더 읽어볼까?(이야기)**
- `동탄이네 이야기` = `blog.html`. 만든 이야기·대처법·소리 적응·여름 대비 **4편의 입구**
- ⚠️ **`동탄이의 소리 적응`을 헤더에 다시 올리지 말 것.** 한 칸을 차지하면 정체성 규칙(1절 — 소리 적응은 선택 안내일 뿐)과 어긋나 훈련 서비스처럼 보인다
- `story.html`·`guide-training.html`은 헤더에서 뺐지만 `index.html` 하단 카드와 `blog.html`에서 계속 직접 링크된다

### 링크 도달성 점검
애드센스는 글 개수보다 **닿을 수 있는 콘텐츠**를 본다. 새 페이지를 만들면 메뉴에 붙였는지 확인할 것.
```bash
for f in $(ls *.html); do
  n=$(grep -l "$f" *.html nav.js | grep -v "^$f$" | wc -l)
  [ "$n" -eq 0 ] && echo "고아: $f"
done
```
> **404.html만 나오는 게 정상이다.**

---

## 4. 작동 구조

```
cron-job.org (5분마다)
   └→ send.yml (workflow_dispatch)
        └→ sender.py : 구독자 읽기 → 기상청 낙뢰 조회 → 조건 맞으면 웹푸시
             └→ (성공 시 workflow_run 트리거)
                  └→ radar.yml → radar.py : 레이더 PNG + 낙뢰 JSON 생성 → Supabase Storage
                                                    ↓
                                    index.html 이 그 파일들을 지도에 표시
```

- **DB**: Supabase (ref `pdlohzenslwbiyoxwjom`), 테이블 `subscribers`
- **이미지**: Supabase Storage 공개 버킷 **`radar`** (git에 넣지 말 것 — 저장소가 부풂)
- **스케줄러**: cron-job.org (GitHub 내장 schedule은 중복 발송 방지로 **미사용**)
- 워크플로우가 **2개로 분리**됨 — 레이더 생성이 실패해도 푸시 발송은 영향 없음

---

## 5. 알림 로직 (⚠️ 2026-08 큰 변경)

### 낙뢰만 발송 — **강수 예보 푸시는 중단됨** (커밋 `7a6bfb7`)
`sender.py`에서 `fetch_forecast`·`RAIN_FORECAST_HOURS`·강수 쿨다운이 **전부 제거**됨.
DB의 `cooldown_min` 칸은 남아있지만 **현재 사용 안 함**(호환용).
강수 예측은 **화면에서만** 보여줌(히어로 탭).

### 거리 기준 + 개수 기준 (둘 다 만족해야 단계로 인정)
| 단계 | 거리 | 개수 | 의미 |
|------|------|------|------|
| `watch` | **50km 이내** | `MIN_STRIKES`(기본 3)건 이상 | 접근 |
| `warning` | **30km 이내** | 〃 | 임박 |

> 🚨 **개수 조건을 없애지 말 것 — 오경보를 냈던 자리다 (2026-08-21)**
>
> 07:15에 용암동으로 "45km 낙뢰 관측" 알림이 나갔다. 그런데 그 시각
> 낙뢰 199건 중 **197건이 서해**(126E 미만)였고, 반경 안에는
> `07:08:39 (36.968N, 127.254E)` **한 발**뿐이었다.
> 뇌우가 다가온 게 아니라 **뇌우에서 멀리 떨어진 곳에 튄 낙뢰**였고,
> 45km면 천둥소리도 안 들린다. 준비할 것이 없는데 알림이 간 것이다.
>
> 원인은 `nearest_strike_km()`으로 **가장 가까운 한 발만 보고** 단계를 정한 것.
> 지금은 `strike_summary()`가 거리와 함께 **반경별 개수**를 세고,
> `MIN_STRIKES` 미만이면 로그만 남기고 보류한다(`45km에 낙뢰 1건 — 3건 미만이라 보류`).
>
> - 진짜 뇌우는 한 주기에 수십~수백 건이라(그날 새벽 128~495건) **지연은 사실상 없다**
> - 너무 둔하다 싶으면 환경변수 `MIN_STRIKES`를 2로 내리면 된다
> - ⚠️ 거리만 좁히는(50→40km) 방식으로는 안 풀린다. 외딴 한 발은 거리와 무관하게 튄다

### 발송 조건 = **최초 관측 + 단계 변화만** (`lightning_transition`)
```
prev=없음 → watch      → "initial_watch"    ⚡ 50km 이내 낙뢰 관측
prev=없음 → warning    → "initial_warning"  🚨 30km 이내 낙뢰 관측
watch    → warning     → "closer"           🚨 가까워졌어요
warning  → watch       → "farther"          ↗️ 멀어졌어요
같은 단계 유지          → 발송 안 함 (None)
낙뢰 사라짐             → 단계 리셋 → 다음 뇌우 때 다시 최초 관측으로
```
> 이전의 "같은 단계 30분 쿨다운" 방식이 **단계 변화 기반으로 교체**됨. 도배가 원천 차단됨.

### ⭐ 낙뢰 조회는 전국 1회 (2026-08-16 변경)
예전에는 **격자마다** 기상청을 호출해서, 가입자가 흩어질수록 호출이 늘었다.
`MAX_GRIDS=300` 설정이면 하루 86,400건 — 기상청 일반회원 한도(20,000건)의 4배다.
한도가 소진되면 **낙뢰 알림 자체가 멈춘다.**

지금은 **전국(반경 500km) 1회만** 받고, 거리 계산은 가입자 각자의 좌표로 직접 한다.
```
격자 수에 비례 (300격자 = 하루 86,400건)  →  하루 288건 고정 (한도의 1.4%)
```
- 가입자가 100명이든 10만 명이든 **호출량이 안 늘어난다**
- 격자로 묶을 이유가 없어져 거리도 **본인 좌표 기준**(예전엔 격자 대표 좌표라 ±3~4km 오차)
- 실측: 반경 50km→2000km로 넓혀도 응답 1.6KB·100ms로 동일. 500km면 한반도 전역
- `sender.cap_grids()`는 남아 있지만 이제 **레이더(radar.py)만** 쓴다

- 알림 탭 시 열리는 곳: `ALERT_CLICK_URL` = `https://thundermiri.com/#radar`
- 거리 수치는 `sender.py` 외에 **index.html·how.html** 에도 하드코딩돼 있으니 함께 고칠 것

---

## 6. 첫 화면 구성 (index.html)

```
┌─ 왼쪽(copy) ────────────┬─ 오른쪽(map) ──────────────┐
│ 헤드라인 + 리드           │ ● 상태 + [↻ 새로고침]        │
│                         │ [⚡최근낙뢰][🌧강수예측][🐕산책]│
├─ 아래(signup) ──────────┤ [지도 + 스타일전환 3버튼]     │
│ [📍 현재 위치로 등록하기]  │ [낙뢰 시간별 색상 범례]        │
│ ── 또는 주소로 찾기 ──    │ [▶] ━━●━━ 시각              │
│ [주소 검색]              │                             │
│ [알림 받기]              │                             │
└─────────────────────────┴─────────────────────────────┘
        ↓
  "동탄이 이야기" 펼친 카드 2장 (만든 이야기 / 소리 적응 기록)
        ↓
  알림 설정 모달(notify-sheet) — iOS 홈화면 추가 안내
```

**히어로 3탭** (`setHeroRadarMode`)
1. **⚡ 최근 낙뢰** (기본) — 최근 1시간 낙뢰를 10분 단위 6구간으로. 최근일수록 빨강+점멸, 재생 가능
2. **🌧️ 강수 예측** — QPF 오버레이 + 타임바
3. **🐕 산책 전 확인** — 앞으로 2시간 비 예보 요약 (`renderWalkWeather`)

**가입폼 (2026-08-16 개편)**
- **위치 입력이 유일한 필수 항목.** `📍 현재 위치로 등록하기`(GPS)가 첫 자리, 실패하면 아래 주소 검색으로 내려가는 구조
  - GPS 오차 3km 초과(주로 PC)면 "지도 핀이 우리 동네가 맞는지 확인" 안내. 한반도 범위(33~39N/124~132E) 밖이면 거부
  - 권한 거부/시간초과는 각각 다른 문구. 위치가 정해지면 `setLocation()`이 안내문을 지움
- **강아지 이름 입력 제거** — 성공 화면·푸시 본문 모두 "우리 아이" 고정. DB `dog_name` 칸은 남아있지만 **아무도 안 씀**(기존 데이터 보존용)
- 주소 라벨은 **한국식 순서**로 정리해 저장: `shortLocationLabel()`이 OpenStreetMap의 좁은곳→넓은곳 순을 뒤집고 행정구역 접미사(도/시/군/구/동/읍/면/리/가)만 골라냄
  → `용암동 상당구 청주시 충청북도` ❌ / `충청북도 청주시 상당구 용암동` ✅
  → 아파트단지명·별칭·도로명·우편번호는 접미사가 안 맞아 자동 탈락

**하단 콘텐츠 (2026-08-16 개편)**
- `더 알아보기` 아코디언 → **`동탄이네 이야기` 펼친 카드 2장.** 접기 제거 = 클릭 1회 삭제, 부제 없앰(카드가 이미 펼쳐져 있어 불필요)
- 요약문 대신 **본문 발췌를 그 자리에서** 읽게 함(체류시간 목적)
  - 왼쪽 `제작스토리`: 침수 사건 도입 + 인용구 + `제작스토리 더보기 →`(story.html)
  - 오른쪽 `보호자의 개인적인 기록`: 4단계 목록은 **카드에서 제거**(guide-training.html에는 그대로 있음). 대신 h3 "변화과정 기록을 더보기 전, 알려드립니다" + 경고 박스 + `변화과정 더보기 →`(guide-training.html)로 경고 먼저 읽게 구성
  - ⚠️ 경고 문구 표현 주의: "완화"를 목적으로 서술하면 정체성 규칙(1번 항목, 금지 표현 "공포를 줄여주는")과 부딪힘. "동탄이도 완전히 극복한 건 아니고, 보호자가 컨트롤할 수 있는 정도의 도움을 받고 있다"처럼 **개인 경과 서술**로 표현할 것

**그 외**
- 위치·구독 상태는 localStorage: `thunder_alert_profile`, `thunder_saved_location`, `thunder_basemap`
- 지도 밖 낙뢰가 있으면 **"지도 밖 낙뢰 위치 안내"** 버튼 표시(`heroLightningViewport`)
- 배경지도 기본 `Base`(일반), 채도 `saturate(.75)`로 낮춰 강수·낙뢰가 도드라지게

---

## 7. 꼭 기억할 함정 ⚠️

1. **시간대(TZ)**: GitHub Actions는 UTC. `send.yml`에 `TZ: Asia/Seoul` **필수**, `sender.py`엔 `KST` 상수 있음. **로컬(KST)에선 정상이라 안 드러남 → 검증은 반드시 클라우드 로그로.**
2. **로컬 실행**: `PYTHONIOENCODING=utf-8 python sender/radar.py` (한글 출력). `.env.secret` 필요.
3. **Supabase 새 키**: `apikey` 헤더에만. `Prefer: return=representation` 쓰지 말 것(RLS 위반).
4. **cron-job.org**: 폼에서 method/헤더 바꾸면 **body가 리셋됨** → body 비면 GitHub 422. 저장 후 body(`{"ref":"main"}`) 재확인.
5. **죽은 구독**: 아이폰 앱 재설치/알림 재설정 시 조용히 무효화(발송은 "성공"인데 폰엔 안 옴). 404/410이면 자동 비활성화.
6. ~~범위·해상도 변경 시 과거 프레임 삭제~~ — 과거 프레임 생성이 중단돼(2026-08-16) 더는 해당 없음. 되살릴 경우 다시 유효해짐.
7. **캐시**: Storage JSON도 캐시됨 → 검증 시 `?cb=타임스탬프`. CSS/JS 바꾸면 `index.html`의 `?v=` 값도 올려야 기존 방문자에게 반영.
8. **CSS 클래스 충돌**: `landing.css`의 `.cta`가 광범위해 헤더 `<a class="cta">`까지 늘어난 적 있음 → `button.cta`로 한정해 해결.
9. **서비스워커(`sw.js`)**: 푸시 표시·클릭 처리 담당. 수정 시 기존 구독자 영향 가능 — 신중히. 최근 "즉시 활성화(skipWaiting)" 추가됨.
10. ⭐ **격자 수 = 비용** (2026-08-16 안전장치 추가)
    - `publishable` 키가 `landing.js`에 공개돼 있고 RLS는 INSERT 무제한 허용 → **누구나 행을 넣을 수 있음**
    - 다만 `get_subscribers()`가 `subscription` 없는 행을 버리므로, **주소만 가짜로 넣는 스팸은 무해**
    - 진짜 위험: `subscription`에 가짜 JSON을 채우고 **`nx/ny`를 여러 격자로 흩뿌리는 것**
      → 두 엔진 모두 격자 단위로 일함. 발송은 격자당 기상청 1회, **레이더는 격자당 ~10회 + 업로드 12개**
      → 기상청 API 한도가 소진되면 **낙뢰 알림 자체가 멈춤** (최악의 시나리오)
    - **안전장치**: `sender.cap_grids()` — 상한 초과 시 **가입이 오래된 순으로만** 처리하고 경고 로그
      | 엔진 | 상수 | 기본값 | 이유 |
      |---|---|---|---|
      | `sender.py` | `MAX_GRIDS` | 300 | 알림은 서비스의 존재 이유 → 넉넉하게 |
      | `radar.py` | `RADAR_MAX_GRIDS` | 50 | 훨씬 무거움 → 낮게 |
    - DB 방어: INSERT 정책에 **좌표 한반도 범위**(33~39N / 124~132E) 조건 추가 → `supabase_schema.sql` 하단
    - 로그에 `[경고] ... 상한 N 초과`가 보이면 **정상 증가인지 스팸인지 `subscribers` 테이블부터 확인**
    - 아직 안 한 것: 푸시 endpoint 도메인 화이트리스트 / endpoint unique 인덱스 / 캡차(가입 마찰 때문에 보류)
11. ✅ ~~`radar.yml` 실행이 가끔 취소됨~~ → **해결 (2026-08-21)**
    - 증상: 5분 주기인데 생성이 5분을 넘겨 다음 실행에 취소됨. 실측 **12회 중 4회**(5~7.5분), 그 주기 레이더가 통째로 사라짐
    - 원인: **느려질 때의 상한이 없었다**
      ```
      레이더 수신  timeout=180초 x 4회  = 최악 12분
      예측 9장     timeout=90초         = 최악 13.5분
      워크플로우   상한 없음
      ```
    - 조치: 수신 `60초x3회`, 예측 `45초`, **`RADAR_DEADLINE_SEC`(기본 210초)** 도입, `radar.yml`에 `timeout-minutes: 4`
    - ⭐ **순서가 핵심이다.** 관측(낙뢰 포함) JSON을 먼저 올리고 예측·동네를 나중에 만든다.
      마감에 걸려 끊겨도 **번개 위치는 항상 남는다** — 알림 받고 들어온 사람이 봐야 할 게 그거라서
    - 정상 소요는 **30초 안팎**(로컬 실측 29초). 다시 5분을 넘기기 시작하면 구독자 증가로 동네 생성이 늘어난 것이니 병렬화를 검토할 것
12. **GitHub Pages는 `index.html`을 약 10분 캐시함** (2026-08-16)
    - `?v=` 캐시버스터는 **CSS·JS에만 효과** 있음. `index.html` 자체(마크업·`?v=` 값 변경 포함)는 못 우회함
    - 배포 직후 확인하면 최대 10분간 **옛 화면**이 보일 수 있음 → 바로 확인하려면 주소 뒤에 `?cb=아무값` 붙이기, 또는 iOS Safari는 탭을 닫았다 새로 열기
    - CSS/JS만 고쳤을 때도 **`index.html`의 `?v=` 값을 반드시 같이 올릴 것** — 안 올리면 이미 방문한 브라우저가 옛 CSS를 계속 씀(2026-08-16 z-index 수정 때 이걸 빠뜨려 배포됐는데도 안 바뀐 것처럼 보였던 사고 있음)
13. ⭐ **Leaflet 지도가 헤더를 덮는 문제 — z-index 스택 컨텍스트** (2026-08-16)
    - `.purpose-map`이 `position:sticky`(데스크톱)나 `static`(모바일)이면서 `z-index`가 없으면(`auto`), 자기 스택 컨텍스트를 안 만듦
    - 그 결과 Leaflet 내부 패널의 `z-index:400`이 카드를 빠져나가 헤더(`z-index:100`)를 그대로 덮음(스크롤해서 지도가 헤더 위로 올라오면 이 증상)
    - 해결: 지도 카드에 `z-index:0` 추가(스택 컨텍스트 생성). 모바일은 `position:static`이면 애초에 z-index가 안 먹으니 `position:relative`도 같이 필요
    - ⚠️ **`position:relative`로 바꾸는 순간, 무시되던 기본 규칙의 `top:86px`(sticky용)가 되살아나 지도가 아래로 밀림.** 반드시 **`top:auto`도 같이** 줘야 함 — 안 그러면 카피~지도 사이에 공백이 생기고 지도가 가입폼과 겹침(실제로 한 번 이 순서를 놓쳐서 배포했다가 바로 되돌린 적 있음)
14. **기상청은 정각에 순간 끊김이 있다** (2026-08-18 실측)
    - `17:00` 정각에 `apihub.kma.go.kr` 접속이 타임아웃(30초 × 3회 재시도 모두 실패). 다음 주기(`17:05`)에 바로 정상 복귀
    - 그래서 **조회 실패 1회는 경보를 보내지 않는다**(`FAIL_ALERT_STREAK=2`). 순간 끊김마다 알리면 곧 무시하게 되고, 그러면 진짜 장애를 놓친다
    - 조회 실패를 빈 목록으로 넘기면 **'낙뢰 없음'과 구별이 안 된다** — 워크플로우는 초록불인데 천둥이 쳐도 알림이 안 나감. `fetch_lightning_data(strict=True)`로 발송 경로만 예외를 올리게 해뒀음(레이더는 관대하게 유지)
15. ⭐ **Supabase 공개 URL은 CDN 캐시 때문에 방금 쓴 값이 안 보인다** (2026-08-18 실측)
    - `storage/v1/object/public/...`에 `?cb=타임스탬프`를 붙여도 직전에 쓴 내용이 그대로 안 읽힘
    - 연속 실패 횟수를 세는 로직이 **한 주기씩 늦게** 반응해서 발견함
    - **쓰고 바로 읽어야 하면 인증 경로**(`storage/v1/object/...` + `sb_headers()`)를 쓸 것. 브라우저에 보여주는 용도만 공개 URL
16. **GitHub Pages 빌드가 GitHub API 504로 실패할 수 있다** (2026-08-17 실제 발생)
    - Jekyll의 `github-metadata` 플러그인이 저장소 정보를 가져오다 죽음. 우리 코드와 무관
    - `.nojekyll` 파일로 Jekyll 자체를 껐다 — 이 사이트는 정적 HTML이라 Jekyll이 필요 없고, 외부 API 호출 경로가 사라져 재발하지 않음
    - 부수 효과: `.md` 파일이 HTML로 변환·공개되던 것도 없어짐(`이어작업_가이드.html` → 404). `.md` 원본은 여전히 접근 가능하고, 저장소가 public이라 GitHub에서도 읽힘

---

## 8. 기상청 API 정보 (어렵게 알아낸 것 — 재조사 금지) 🔑

### 승인 현황
- **레이더 카테고리 11건** (~2028-08-11) — API허브 `seqApi=5`
- **초단기예측(QPF)** — API허브 **`seqApi=9&seqApiSub=282`** (수치모델 > 초단기예측). ⚠️ 레이더와 **별개 카테고리**
- 낙뢰관측은 원래부터 사용 중

### 낙뢰 (관측)
```
https://apihub.kma.go.kr/api/typ01/url/lgt_pnt.php
params: tm(YYYYMMDDHHMM), itv(분), lon, lat, range(km), gc=T, authKey
```
- ⭐ `itv`는 **데이터 신선도가 아니라 조회 창 크기**. 지점 데이터는 **초 단위**(10분 단위 아님 — 실측 확인)
- 🚨 **`itv`는 60 이상이면 최근 자료를 안 준다** (2026-08-21 실측 — 실제 버그를 냈던 함정)
  | itv | 반환 구간 | 결과 |
  |---|---|---|
  | 15~59 | `[지금-itv, 지금]` | 정상 |
  | **60** | — | **0건** (HTTP 200에 데이터 행만 없음) |
  | 61 이상 | `[지금-itv, 지금-65]` | **최근 65분이 통째로 빠짐** |

  `radar.py`의 전국 낙뢰 조회가 하필 `lookback_minutes=60`이라 **화면 레이더의 낙뢰가 거의 항상 비어 있었다.**
  발송은 `LOOKBACK_MINUTES=15`라 무사했다(알림은 정상 작동 중이었음).
  → `fetch_lightning_data()`가 **60 이상이면 59로 낮추고 로그를 남긴다.** 실질 상한은 **59**다.

  > ⚠️ 2026-08-19에 "`itv`는 `[tm-itv, tm+itv]` 양쪽 구간"이라고 적었던 건 **과거 tm으로 재서 생긴 오해**다.
  > 과거 시각을 넣으면 이미 쌓인 자료라 앞뒤가 다 나오지만, **지금 시각으로 조회하면 위 표대로 동작한다.**

- ⭐ **공개 지연 실측 완료 (2026-08-19 20:19~20:24, 낙뢰 활발한 중에 측정)**
  | 항목 | 값 |
  |---|---|
  | 공개 단위 | **2분** — 최신 tm이 2분 단위로 뛰고 `:59`로 끝난다 |
  | 공개 지연 | **약 4.3분** (최소 259초) |
  | 번개 → 알림 총 소요 | **5~9분** (기상청 4.3분 + 우리 주기 최대 5분) |

  관측 기록:
  ```
  20:19:17 ~ 20:21:03   최신 = 20:14:59   (4회 연속 동일)
  20:21:38 ~ 20:24:34   최신 = 20:16:59   (6회 연속 동일)
  ```
  - 재는 법: **낙뢰가 치는 중에만** 가능하다. 30~40초 간격으로 여러 번 조회해
    `now - max(tm)`의 **최솟값**을 본다. 한 번만 재면 "그냥 그동안 번개가 안 친 것"과 구별이 안 된다
  - 개별 낙뢰의 tm은 초 단위지만, **공개는 2분치씩 묶어서** 이뤄진다
  - 이 수치를 근거로 화면 문구를 `5분마다 확인하는 최근 낙뢰 관측`으로 정정함
    (옛 문구 "10분 단위로 확인하는"은 우리가 10분마다 본다는 뜻으로 읽혀 틀렸다)
- 10분 단위인 건 날씨누리 화면·격자 상품 쪽
- 응답 인코딩 euc-kr

### 레이더 합성영상 (관측)
```
https://apihub.kma.go.kr/api/typ01/cgi-bin/url/nph-rdr_cmp1_api
params: tm(생략 시 종종 "자료 없음" → 명시 권장), cmp=HSR, qcd=KMA,
        obs=ECHO, map=HB, disp=B(binary 13MB / A=ASCII 47MB)
응답: 앞 4byte = nx,ny(little-endian short), 이후 nx*ny개 short
     row 0 = 남쪽(flipud 필요), 값 = dBZ*100
     -30000 = 관측영역 밖, -25000 = 에코 없음
map=HB 격자 = 2305 x 2881, 0.5km (1152.5 x 1440.5 km)
```

### ⭐ 좌표 투영 (제일 어려웠던 부분, 검증 완료)
```
+proj=lcc +lat_1=30 +lat_2=60 +lat_0=38 +lon_0=126 +x_0=0 +y_0=0 +ellps=WGS84 +units=km
격자중심 = 투영원점.  ix = x/0.5 + nx/2,  iy = y/0.5 + ny/2
```
검증: 청주·서울·부산·제주·울릉도 전부 관측영역 안, 동해먼바다(133.5E)·중국(113E)은 격자 밖. `pip install pyproj` 필요.

### 미래 강수예측 QPF
```
https://apihub.kma.go.kr/api/typ03/cgi/dfs/nph-qpf_ana_img
params: eva=2, tm(10분단위), qpf=B(융합예측; K=초단기모델, M=레이더외삽),
        ef=예측시간(분), map=HB, grid=2, legend=0, size=600
QPF_STEPS 기본 = 30,60,90,120,150,180,240,300,360
```
- **응답은 기상청이 렌더한 완성 PNG**(해안선·범례 포함, 불투명)
- ⭐ **좌표 실측 확정**: `map=HB, legend=0, size=600` → **600 x 770 PNG**.
  **20행·769행이 지도 테두리선** → 지도영역 = **rows 20..769(750px) x cols 0..599(600px)**.
  이 750x600이 **레이더 HB 격자와 동일**(종횡비 0.8001 vs 0.8).
  ```
  col = (x_km + 1152.5/2)/1152.5*600
  row = 20 + (1440.5/2 - y_km)/1440.5*750
  ```
  검증: 서울·부산·울릉도는 해안선 위, 먼바다는 빈 곳. 예측 bounds가 관측 bounds와 **0.3km 오차**.
- **투명 오버레이 변환**: 채도(max−min RGB) **> 30인 픽셀만 = 비**로 골라내고 나머지 투명
- ef=0은 거의 빈 이미지(782B)라 제외. 유효 프레임은 3000바이트 초과로 판정

### ⚠️ 절대 쓰지 말 것
날씨누리 GIS 모드는 **내부 전용 API**(`vapi.kma.go.kr/capi/url/vs_10minutes_rain_timeline.php`) 사용. API허브에 없는 비공개 주소 → **무단 사용 금지**(차단·변경 위험, 광고 수익 서비스엔 특히).

### 낙뢰만으론 이동방향 자동 판별 불가 (검증 완료 — 재시도 말 것)
- 5분 창 최단거리가 ±40~60km씩 튐(뇌우 안에서 번개가 무작위로 터지는 점 과정)
- 20~30분 창으로 평균내면 추세는 보이나 판단이 20~30분 늦어 경보용 부적합
- → **강수 레이더(덩어리로 움직임)가 방향 판단의 핵심.** 과거(낙뢰)와 미래(레이더)는 충돌이 아니라 상호보완

---

## 9. 브이월드(국토교통부) 지도

- 키: `12A21CCD-E7FC-3E4E-B6A5-F8DE7A32A24D` (도메인 `kustomduo-oss.github.io` 잠김 → 공개돼도 안전)
- ⚠️ **개발키라 2027-02-11 만료**, 최대 3회 연장. **만료되면 지도가 조용히 안 뜸.** 운영키(2년)는 심사 필요
- 타일: `https://api.vworld.kr/req/wmts/1.0.0/{KEY}/{TYPE}/{z}/{y}/{x}.png`
- 사용 가능 TYPE 실측(**브라우저에서 테스트** — curl은 도메인 제한으로 무의미):
  - ✅ `Base`(일반) · `white`(밝게) · `midnight`(어둡게) · `Hybrid`
  - ❌ `Satellite`(권한 없음, 추가 신청 필요) · `gray`(서비스 종료)
- 실패 시 OpenStreetMap 자동 폴백 구현돼 있음

---

## 10. Supabase Storage 파일 규칙

| 파일 | 내용 |
|------|------|
| `national.png` / `national.json` | 전국 관측(가입 전 방문자용) |
| `national_forecast.json` + `national_qpf_{ef}.png` | 전국 예측 |
| `{nx}_{ny}.png` / `.json` | 동네 관측(최신) |
| `{nx}_{ny}_forecast.json` + `{nx}_{ny}_qpf_{ef}.png` | 동네 예측 |

> ~~`{nx}_{ny}_{YYYYMMDDHHMM}.png` 과거 프레임~~ — **2026-08-16 생성 중단**. 이 파일을 재생하던 `radar.html`이 삭제돼 읽는 곳이 없어졌음. meta의 `past`는 빈 배열로 유지(호환용).
> ⚠️ **낙뢰와 무관함**: 과거 프레임은 **강수 관측** 이미지였고, 낙뢰는 meta의 `lightning[]`에 매회 최근 60분치가 통째로 들어옴(`radar.py`의 `lookback_minutes=60`). 히어로 낙뢰 재생은 영향 없음.

- meta JSON: `bounds`(위경도 4모서리)·`home`·`rain_cells`·`max_dbz`·`observed_at`·`lightning[]`
- 예측 프레임엔 `rain_px` — 0이면 "이 시각엔 예보된 비가 없습니다" 표시(고장 오인 방지)
- 공개 URL: `{SUPABASE_URL}/storage/v1/object/public/radar/{파일명}`

### ⭐ 위경도 재투영 필수
LCC 격자를 위경도 사각형에 그대로 얹으면 좁을 땐 괜찮지만 **넓히면 한반도가 비뚤어짐**.
`view_bounds()` + `latlon_sample_grid()`로 관측·예측 **둘 다 동일한 위경도 격자에 재투영**.
표시 범위 = **±520km**(`RADAR_CROP_KM`), 출력 **900px**(`RADAR_OUT_PX`).

> `heartbeat.json`도 이 버킷에 있다. 발송 감시용이며 레이더와 무관 — 아래 11절 참고.

---

## 11. 장애 감시 (2026-08-18 추가) 🚨

**알림이 멈춰도 아무도 모르는 상태**였다. 특히 기상청 조회 실패가 '낙뢰 없음'과
구별되지 않아, 워크플로우는 초록불인데 천둥이 쳐도 알림이 안 나갔다.

경보는 **관리자(사장님) 1인에게만** 간다. 사용자 웹푸시와는 완전히 별개다.

| 장치 | 잡는 상황 | 어디서 |
|------|----------|--------|
| 조회 실패 구분 | 기상청 장애·한도 소진 | `sender.py` — 연속 2회부터 |
| 워크플로우 실패 알림 | 코드 크래시, 인프라 장애 | `send.yml`의 `if: failure()` |
| 심장박동 | cron-job.org 중단, Actions 장애 | `heartbeat.json` 공백 감지 |

**텔레그램** — 동탄이 봇(`lightning-alert` 저장소)을 재사용한다. 새로 만들 필요 없음.
- 시크릿: `TELEGRAM_TOKEN`, `TELEGRAM_CHAT_ID`
- CHAT_ID = **`8795860239`** (`lightning-alert`의 `설치가이드.md`에 기록돼 있음)
- TOKEN은 텔레그램 `@BotFather` → `/mybots` → 봇 선택 → `API Token`
- ⚠️ GitHub 시크릿은 **저장 후 다시 볼 수 없다.** 다른 저장소에 넣어둔 값도 못 꺼냄
- 값이 없으면 **경보만 건너뛰고 발송은 정상 진행**한다(안전하게 설계됨)

**심장박동 파일** `radar/heartbeat.json`
```json
{"last_success":"2026-08-18T17:07:08+09:00","sent":0,"strikes":0,"fail_streak":0}
```
- 성공할 때마다 갱신, `fail_streak`은 0으로 초기화
- 실패 시엔 `last_success`를 **건드리지 않는다**(공백 감지의 기준이라서)
- 다음 실행이 `STALE_MINUTES`(기본 15분) 이상 공백을 발견하면 몇 회 걸렀는지 역산해 알림
- 완전 장애 중에는 조용하지만, **복구되는 순간** 무슨 일이 있었는지 알려준다

**조정 가능한 값** (환경변수)
| 이름 | 기본 | 뜻 |
|---|---|---|
| `FAIL_ALERT_STREAK` | 2 | 조회 실패 몇 회 연속부터 경보 |
| `STALE_MINUTES` | 15 | 이만큼 공백이면 "멈춰 있었다"로 판단 |

경보 장치를 고친 뒤에는 **반드시 실물 테스트**할 것 — "설정했는데 정작 안 울리는 것"이
최악이다. 일회용 워크플로우로 일부러 실패시켜 확인하고 지우는 방식을 썼다.

---

## 12. 사용자 작업 스타일 (중요)

- **한국어로 응답**
- 웹개발이 처음 → **매 단계 "지금 뭐 하는 중인지 + 쉬운 비유"** 곁들일 것
- 승인 문화: 코드 수정 전 **제안 → 승인** ("ㅇㅇ", "해봐", "진행해" 등)
- **추측하지 말고 실제로 측정·검증**하는 걸 선호. 여러 번 "실측했더니 가설이 틀렸다"로 방향이 바뀜
- 잘못된 제안을 하면 **정직하게 인정하고 방향 수정**할 것 — 여러 번 사용자 판단이 더 옳았음
  (예: "클릭 한 번의 허들도 없애는 게 낫다" → 레이더를 첫 화면에 넣은 결정)
- 사업 관점: **체류시간 → 광고 수익** 중시. "알림만 받고 끝나면 서비스가 거기서 끝난다"

---

## 13. 확장 시 병목

- 첫 병목은 서버가 아니라 **`sender.py`의 순차 발송 루프(약 1,000~2,000명)** → 병렬 발송으로 1만명대까지
- GitHub Pages/Supabase/Actions는 훨씬 나중 문제
- 1만명+ 되면 Cloudflare(Workers+R2+Pages) 검토 — 그때쯤 광고 수익이 비용을 넘음

---

## 14. 남은 할 일

- 🔲 **모바일 화면 점검** (미확인 — 실사용자 대부분이 폰, 최우선)
- ✅ ~~`radar.html` 중복 판단~~ → **삭제 완료 (2026-08-16)**. 링크가 하나도 없는 고아 페이지였음. 레이더 화면은 `index.html#radar` 하나로 통일
- ✅ ~~과거 관측 프레임 생성 중단~~ → **완료 (2026-08-16)**. `radar.py`에서 시각별 사본 업로드와 `_keep_recent_frames()` 제거
  - 되살리고 싶으면(= 비구름 되감기 기능): 원본 코드는 `git show d11296a~1:radar.html` 437행 부근 병합 로직 참고 → `landing.js` 히어로 타임바에 이식
- ✅ ~~Storage에 남은 과거 프레임 삭제~~ → **완료 (2026-08-16)**. 57개 삭제, 버킷 81개 → 24개. 현역 파일 6종 응답 200 확인
  - 도구는 남겨둠(`sender/cleanup_past_frames.py`). 나중에 비슷한 정리가 필요하면 재사용
  ```bash
  python sender/cleanup_past_frames.py            # 세기만 (안전)
  python sender/cleanup_past_frames.py --delete   # 실제 삭제
  ```
  - 대상 규칙: `{key}_{12자리숫자}.png` 만
  - `national.*` / `*_qpf_*.png` / `*_forecast.json` / `{key}.png|json` 은 **현역이므로 건드리지 않음**
  - `sender/.env.secret` 필요. 실행 위치는 상관없음
- ✅ ~~죽은 구독 정리~~ → **완료 (2026-08-16)**. 기존 12행 전부 `active=false`로 전환(행은 안 지움, 재가입하면 새 행 생성). 발송 대상 0명 확인
- ✅ ~~분석 도구(GA4 · Clarity) 설치~~ → **완료 (2026-08-19)**
  | 도구 | ID | 위치 |
  |---|---|---|
  | GA4 | `G-MN7NNJ3B7H` | 계정 `kustomduo` > 속성 `thundermiri.com` (550684082) |
  | Clarity | `y4jh2weiie` | clarity.microsoft.com (kustomduo@gmail.com, Google 로그인) |
  - **주입 위치는 `nav.js` 한 곳** — 15개 페이지가 전부 nav.js를 부르므로 여기만 고치면 전체 반영
  - `LIVE_HOSTS`에 있는 주소에서만 수집 → localhost·file:// 테스트는 통계에 안 섞임
  - GA4 **향상된 측정 ON** — 페이지뷰·**스크롤**·이탈 클릭 자동 수집(스크롤 가설 검증에 코드 추가 불필요)
  - ⚠️ 처음 GA 홈에 보이는 `G-4NCYRF3QJ0`은 **"명리학 공부노트"** 것 — 헷갈리지 말 것
  - ⚠️ 도구를 바꾸면 `cookies.html`·`privacy.html`도 같이 고칠 것(고지 불일치는 애드센스 감점)
  - Clarity 마스킹 **엄격한(Strict)** 적용 완료 — 모든 텍스트가 서버로 전송되지 않음(가입 폼에 주소가 있어서).
    히트맵·스크롤 깊이는 영향 없으므로 분석 목적은 그대로 달성됨. 변경 반영에 최대 1시간
  - **쿠키 동의 배너는 달지 않기로 결정 (2026-08-19)** — 다시 꺼내지 말 것.
    기상청 낙뢰 자료가 한반도 격자 전용이라 해외 방문자는 받을 알림이 없고(좌표 정책도 33~39N/124~132E 밖은 저장 거부),
    한국은 고지+거부수단 안내로 충족된다. 배너는 가입 전환만 깎는다.
    ↳ 다시 검토할 시점은 **애드센스로 맞춤형 광고를 켤 때** 하나뿐
- ✅ ~~고아 페이지 3개~~ → **연결 완료 (2026-08-19)**. `how.html`은 헤더 '작동 방식', `blog.html`은 푸터 '글 모음'에 붙였고 `research.html`은 how를 통해 도달 가능해짐
  - 점검 방법: 각 html이 다른 파일에서 언급되는지 세면 된다. **404.html만 고아로 남는 게 정상**
- ✅ ~~커스텀 도메인~~ → **완료 (2026-08-16)**. `thundermiri.com` (2절 참고)
- ✅ ~~기상청 호출량 문제~~ → **해결 (2026-08-16)**. 격자별 조회를 전국 1회로 바꿔 하루 288건 고정 (8절 참고)
- ✅ ~~장애 감시~~ → **완료 (2026-08-18)**. 텔레그램 경보 3종 (11절 참고)
- 🔲 **푸시 병렬 발송** — 지금은 한 명씩 순차. 1,000명이면 2~5분이라 5분 주기에 걸린다.
  `ThreadPoolExecutor(20~30)`으로 바꾸면 10초대로 떨어지고 1만 명까지 간다. 비용 0원.
  구독자가 실제로 붙기 시작하면 그때 해도 늦지 않음
- 🔲 콘텐츠 더 쌓기 (애드센스 승인용)
- 🔲 브이월드 위성영상 API 추가 신청(원하면)
- 🔲 첫 사용자 모으기

---
*사람과 Claude 둘 다 읽으라고 만든 핸드오프 문서입니다. 프로젝트가 바뀌면 이 파일도 갱신하세요.*
